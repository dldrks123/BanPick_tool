# ------------------------------------------------------------
# 1) 운영체제(OS) 및 에디터별 불필요 파일
# ------------------------------------------------------------

# macOS: Finder가 생성하는 .DS_Store 등
.DS_Store
# Windows: 썸네일 등
Thumbs.db

# VSCode 사용자 설정 폴더
.vscode/

# ------------------------------------------------------------
# 2) Python 백엔드 관련
# ------------------------------------------------------------

# Python 가상환경 폴더
.venv/
venv/
env/

# __pycache__ 디렉터리(파이썬 바이트코드 캐시)
__pycache__/
*.py[cod]

# Pylint, mypy 등 타입 검사에서 생성하는 캐시/로그
.mypy_cache/
.pylint.d/
.pytest_cache/

# 파이썬 로그 파일 (개발 중 생성될 수 있음)
*.log

# ------------------------------------------------------------
# 3) Node/Vue 프론트엔드 관련
# ------------------------------------------------------------

# Node.js 의존성 디렉터리
frontend/node_modules/

# Vite / Vue 빌드 산출물 (dist, .vite, build 등)
frontend/dist/
frontend/.vite/
frontend/build/

# npm, yarn 잠금 파일 (개인 환경에 맞게 사용한다면 생략 가능)
# package-lock.json
# yarn.lock

# ------------------------------------------------------------
# 4) 환경 설정 파일
# ------------------------------------------------------------

# Git에 올리면 안 되는 개인 환경 변수 파일
*.env
*.env.local
backend/.env
frontend/.env

# OS가 임시로 생성하는 파일
*.tmp

# ------------------------------------------------------------
# 5) 기타 IDE/툴 캐시
# ------------------------------------------------------------

# JetBrains 계열 IDE (PyCharm 등) 캐시
.idea/

# 기타 에디터나 빌드 툴이 생성하는 임시 파일
*.swp
*.swo
*.bak
